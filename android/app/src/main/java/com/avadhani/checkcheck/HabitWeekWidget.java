package com.avadhani.checkcheck;

/** Seven-day strip for one habit. All behaviour is in HabitWidgetBase. */
public class HabitWeekWidget extends HabitWidgetBase {
    @Override
    protected boolean isMonth() {
        return false;
    }
}
