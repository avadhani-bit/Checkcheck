package com.avadhani.checkcheck;

/** Month grid for one habit. All behaviour is in HabitWidgetBase. */
public class HabitMonthWidget extends HabitWidgetBase {
    @Override
    protected boolean isMonth() {
        return true;
    }
}
