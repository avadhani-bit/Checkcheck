<?xml version="1.0" encoding="utf-8"?>
<!-- Compact by design.

     The first version gave the ImageView layout_weight="1" with
     scaleType="fitCenter", which told it to fill all remaining height and
     scale the bitmap up to match. On a 4-row-tall widget that blew a small
     calendar up to poster size and made it read like a web page dropped on
     the home screen.

     Now: the grid keeps its natural size (wrap_content + centerInside, which
     only ever scales DOWN), and the widget asks for little space. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/habit_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="10dp">

    <TextView
        android:id="@+id/habit_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="#111827"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/habit_sub"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="10sp"
        android:textColor="#9CA3AF"
        android:paddingBottom="7dp"
        android:maxLines="1"
        android:ellipsize="end" />

    <ImageView
        android:id="@+id/habit_grid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:scaleType="centerInside"
        android:contentDescription="@null" />

</LinearLayout>
